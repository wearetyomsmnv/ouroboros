# Tool Gap: GitHub CLI (gh)

The `gh` CLI tool is missing from the environment.
- **Impact**: Cannot list/read/comment on GitHub issues.
- **Workaround**: None for issues currently.
- **Action**: Need to request installation or find a python library alternative if this persists.
The `gh` CLI tool is missing from the environment.

**Impact**: Cannot list/read/comment on GitHub issues as per system prompt.
**Status**: Confirmed in session `1a8e1c3dc2554eb094691ebaa86c512d`.
**Action**: Need to install `gh` or use `requests` to hit GitHub API directly if permissible/token available.